# 파이썬 한입 교실 구현 계획

- 작성일: 2026-08-31
- 대상: 파이썬과 프로그래밍을 처음 배우는 중학생
- 기준 자료: `byte_of_python.pdf`의 2장 소개부터 11장 실생활 문제 해결까지
- 구현 범위: 정적 웹앱 MVP 전체 구현, 로컬 검증까지
- 제외 범위: 계정, 서버, 학급 관리, 점수 경쟁, 음성 안내/TTS/녹음, 커밋·푸시·배포

## 1. 제품 목표

중학생이 설명을 읽은 직후 같은 화면에서 코드를 고치고 실행 결과를 확인하며, 파이썬의 핵심 문법을 실제 문제 해결까지 연결하도록 한다.

학습 성공 기준은 다음과 같다.

1. 학생이 별도 Python 설치 없이 첫 코드를 1분 안에 실행할 수 있다.
2. 2장부터 11장까지 현재 위치와 다음 학습 행동을 항상 알 수 있다.
3. 실행 결과와 오류가 코드 편집기 바로 아래에서 명확하게 구분된다.
4. 각 장에서 최소 한 번은 예제를 직접 수정하거나 도전 과제를 해결한다.
5. 마지막 장에서 요구 사항 분석 → 작은 구현 → 실행 확인 → 개선 과정을 경험한다.

## 2. 학습 구조

### 2. 파이썬 소개

- 학습 목표: 파이썬이 무엇이며 왜 배우기 쉬운지 설명한다.
- 핵심 내용: 고수준 언어, 인터프리터, 이식성, 오픈 소스, 활용 분야
- 실습: `print("파이썬으로 무엇을 만들까?")`
- 도전: 출력 문장을 자신의 관심 분야로 바꾼다.

### 3. 설치와 실행 환경

- 학습 목표: 브라우저 실습 환경과 실제 PC 환경의 차이를 안다.
- 핵심 내용: 이 앱에서는 설치 없이 실행, 로컬 Python 설치는 확장 학습으로 안내
- 실습: 실행기 준비 상태 확인 및 첫 코드 실행
- 도전: 여러 줄을 차례로 출력한다.

### 4. 첫걸음

- 학습 목표: `print()`와 소스 코드의 실행 순서를 이해한다.
- 핵심 내용: 문자열, 괄호, 따옴표, 위에서 아래로 실행
- 실습: 자기소개 세 줄 출력
- 도전: 의도적으로 따옴표 오류를 만들고 오류 메시지를 읽은 뒤 고친다.

### 5. 기초

- 학습 목표: 변수와 기본 자료형을 사용한다.
- 핵심 내용: 주석, 숫자, 문자열, 변수, 식별자, 자료형, 들여쓰기
- 실습: 이름·학년·좋아하는 과목 저장 및 출력
- 도전: 섭씨 온도를 화씨로 변환한다.

### 6. 연산자와 수식

- 학습 목표: 산술·비교·논리 연산자를 조합한다.
- 핵심 내용: 연산자, 복합 할당, 연산 우선순위, 괄호
- 실습: 간식 예산 계산기
- 도전: 평균 점수와 목표 달성 여부를 계산한다.

### 7. 흐름 제어

- 학습 목표: 조건과 반복으로 실행 흐름을 바꾼다.
- 핵심 내용: `if/elif/else`, `while`, `for`, `break`, `continue`
- 실습: 점수 구간 안내, 구구단, 특정 값 건너뛰기
- 도전: 1부터 30까지 조건을 만족하는 수만 출력한다.

### 8. 함수

- 학습 목표: 반복되는 일을 함수로 묶고 값을 반환한다.
- 핵심 내용: 정의와 호출, 매개변수, 지역 변수, 기본 인수, 키워드 인수, `return`, 문서 문자열
- 실습: 두 수의 평균을 구하는 함수
- 도전: 할인 가격 계산 함수를 완성한다.

### 9. 모듈

- 학습 목표: 이미 만들어진 기능을 가져와 사용한다.
- 핵심 내용: `import`, `from ... import`, `__name__`, 사용자 모듈의 개념, `dir()`
- 실습: `math` 모듈로 원의 넓이 계산
- 도전: `random`을 이용한 결정 도우미
- 제한 안내: 브라우저에서 지원하지 않는 운영체제·네트워크 모듈이 있음을 설명한다.

### 10. 자료 구조

- 학습 목표: 여러 값을 목적에 맞는 구조로 저장하고 순회한다.
- 핵심 내용: 리스트, 튜플, 딕셔너리, 집합, 참조, 문자열 메서드
- 실습: 할 일 목록과 과목별 점수표
- 도전: 설문 응답의 중복을 제거하고 빈도를 센다.

### 11. 실생활 문제 해결

- 학습 목표: 문제를 작은 단계로 나누고 프로그램을 반복 개선한다.
- 프로젝트: 학교 축제 준비물 관리 프로그램
- 1단계: 요구 사항과 예시 입출력 확인
- 2단계: 준비물 목록과 수량 저장
- 3단계: 부족한 준비물 판정
- 4단계: 함수와 자료 구조로 리팩터링
- 5단계: 테스트 사례 실행 및 개선점 기록
- 완료 산출물: 학생이 수정 가능한 하나의 종합 Python 프로그램

## 3. 공통 수업 단위

각 소단원은 아래 순서로 고정한다.

```text
학습 목표 → 핵심 개념 → 짧은 예제 → 직접 실행 → 한 줄 바꾸기
        → 도전 과제 → 힌트 → 결과 확인 → 다음 학습
```

- 정답을 바로 노출하지 않고 1차 힌트와 예시 결과를 제공한다.
- 실행 횟수나 오답 횟수로 학생을 평가하지 않는다.
- 완료 상태는 학생이 직접 `학습 완료`를 누를 때 저장한다.
- 중요한 다음 행동 하나에만 `gi-pulse` 강조를 적용한다.
- `prefers-reduced-motion`에서는 깜빡임을 제거하고 정적 테두리 강조로 바꾼다.

## 4. 화면 설계

### 데스크톱(1024px 이상)

```text
┌──────────────┬─────────────────────────────┬────────────────────────────┐
│ 왼쪽 목차    │ 가운데 학습 내용            │ 오른쪽 코드 편집기          │
│ 240~280px    │ minmax(360px, 1fr)          │ minmax(420px, 0.9fr)        │
│              │                             ├────────────────────────────┤
│ 장/소단원    │ 목표·개념·예시·도전         │ 실행 결과 / 오류 / 입력     │
│ 진행 상태    │ 이전·완료·다음              │                            │
└──────────────┴─────────────────────────────┴────────────────────────────┘
```

- 전체 화면 높이를 사용하되 각 영역은 독립적으로 스크롤한다.
- 코드 편집기와 출력 창 사이 분할 비율을 드래그로 조절할 수 있게 한다.
- 오른쪽 상단에 실행, 중지, 초기화 버튼을 둔다.
- 출력 창은 표준 출력, 오류, 실행 상태를 색과 텍스트로 함께 구분한다.

### 태블릿(768~1023px)

- 목차는 접을 수 있는 왼쪽 서랍으로 바꾼다.
- 학습 내용과 실습 영역은 46:54 비율의 2열을 유지한다.
- 화면 폭이 부족하면 실습 영역을 아래로 내려 세로 흐름으로 전환한다.

### 모바일(767px 이하)

- 상단에 현재 장과 진도, 목차 열기 버튼을 표시한다.
- `학습 내용`과 `코드 실습` 두 탭으로 전환한다.
- 실행 후 자동으로 실습 탭의 출력 창을 화면에 보이게 한다.
- 주요 터치 대상은 최소 44px로 한다.

### 시각 방향

- Visual thesis: 따뜻한 노트 종이와 정돈된 코딩 실험실이 결합된, 밝고 차분한 중학생용 학습 작업대
- 색상: 잉크 네이비, 종이 아이보리, 실행 민트, 강조 코랄 한 가지
- 글꼴: 한국어 본문용 가독성 서체 1종과 코드용 고정폭 서체 1종
- 장식보다 학습 흐름과 코드 가독성을 우선한다.
- 개념 일러스트는 첫 화면의 시각적 방향을 잡는 역할로만 사용하고, 코드·수치·사실 도식은 실제 DOM 텍스트로 제공한다.

### 상시 UI

- 왼쪽 하단: 전체 진도와 초기화
- 상단: 앱 이름, 현재 장, 목차 토글
- 화면 한쪽: `업데이트 내역` 버튼
- 업데이트 내역: 개발일, 개선일, 짧은 변경점, 교재/라이선스 출처

## 5. 콘텐츠 모델

학습 콘텐츠는 UI 코드와 분리하고 TypeScript 데이터로 관리한다.

```ts
type LessonId = `chapter-${number}-${number}`;

interface Lesson {
  id: LessonId;
  chapter: number;
  order: number;
  title: string;
  summary: string;
  objectives: string[];
  concepts: ConceptBlock[];
  starterCode: string;
  expectedOutput?: string;
  challenge: Challenge;
  glossary: GlossaryItem[];
}

interface ConceptBlock {
  type: 'explanation' | 'tip' | 'warning' | 'example';
  title?: string;
  body: string;
  code?: string;
}

interface Challenge {
  prompt: string;
  hint: string;
  checks?: OutputCheck[];
}

interface OutputCheck {
  mode: 'contains' | 'equals' | 'regex';
  value: string;
  feedback: string;
}
```

- 장 메타데이터, 소단원 데이터, 용어집을 별도 파일로 나눈다.
- 한 파일이 500줄에 가까워지기 전에 장별 파일로 분리한다.
- 2015년 PDF 문장을 길게 복제하지 않고 중학생용으로 재서술한다.
- 교재 원저자·번역자·원문·CC BY-SA 4.0을 업데이트/출처 화면에 표시한다.
- 코드 예제의 별도 BSD 조건도 출처 화면에 명시한다.

## 6. 코드 실행기 설계

### 실행 구조

```text
CodeMirror 편집기
      │ run(code, inputQueue)
      ▼
Python Worker ── Pyodide 초기화/실행
      │ stdout / stderr / result / input-request
      ▼
출력 콘솔 + 학습 피드백
```

- Pyodide는 메인 UI와 분리된 module Web Worker에서 초기화한다.
- 첫 실행 시 `Python 준비 중` 상태와 진행 안내를 표시한다.
- `stdout`과 `stderr`를 줄 단위로 UI에 전달한다.
- 실행마다 이전 사용자 전역 변수를 정리해 수업 간 오염을 막는다.
- 기본 표준 라이브러리만 허용하고 사용자 코드에서 임의 패키지 다운로드는 하지 않는다.

### `input()` 처리

- Worker가 입력 요청을 보내면 출력 창 안에 입력 필드를 표시한다.
- 학생이 Enter를 누르면 입력 큐로 전달하고 실행을 계속한다.
- 입력 대기 중에는 실행 버튼을 비활성화하고 중지 버튼을 유지한다.
- MVP에서 안정적인 동기 입력 처리가 어렵다면, 초기 콘텐츠는 `input()` 없이 실행되도록 하고 입력 예제는 사전 정의 값으로 대체한다. 이 경우 UI에 제한을 명시한다.

### 안전장치

- 실행 제한 시간: 기본 5초
- 제한 시간을 넘기거나 학생이 중지를 누르면 Worker를 종료하고 새 Worker를 만든다.
- 출력 제한: 최대 300줄 또는 30KB, 초과 시 실행 중단과 안내 표시
- 연속 실행 잠금: 하나의 코드만 실행하고 중복 클릭을 막는다.
- 패키지 설치, 네트워크 요청, DOM 직접 접근을 학습 범위에서 제공하지 않는다.
- 오류 원문은 보존하되 흔한 오류는 한국어 도움말로 보충한다.
- 사용자 코드는 서버로 전송하지 않고 브라우저 안에서만 실행한다.
- 진도와 마지막 코드는 `localStorage`에만 저장하며 이름·이메일 등 개인정보를 받지 않는다.

### 오류 도움말

- `SyntaxError`: 괄호·따옴표·콜론 확인
- `IndentationError`: 들여쓰기 칸 수와 블록 위치 확인
- `NameError`: 변수 이름과 정의 순서 확인
- `TypeError`: 서로 다른 자료형의 연산 확인
- `ZeroDivisionError`: 나누는 값이 0인지 확인

## 7. 기술 구조와 파일 후보

- Vite + React + strict TypeScript
- CodeMirror 6 Python language extension
- Pyodide + module Web Worker
- Vitest + Testing Library
- 브라우저 검증은 Playwright CLI 사용
- 외부 상태 관리 라이브러리 없이 React state/context로 시작

```text
src/
  app/
    App.tsx
    routes.ts
  components/
    AppHeader.tsx
    ChapterNav.tsx
    UpdateHistoryDialog.tsx
  content/
    chapters.ts
    chapter02.ts ... chapter11.ts
    glossary.ts
  features/learn/
    LessonWorkspace.tsx
    LessonContent.tsx
    ProgressControls.tsx
  features/playground/
    CodeEditor.tsx
    OutputConsole.tsx
    PlaygroundPanel.tsx
    python.worker.ts
    pythonRunner.ts
    errorGuidance.ts
  hooks/
    useLearningProgress.ts
    usePythonRunner.ts
  styles/
    tokens.css
    layout.css
    components.css
  test/
    setup.ts
public/
  assets/
work/
  python-education-app-plan.md
```

## 8. 구현 순서

1. Vite/React/TypeScript 프로젝트와 기본 검사 명령을 만든다.
2. 디자인 토큰과 데스크톱·태블릿·모바일 레이아웃을 만든다.
3. 장·소단원 콘텐츠 모델과 2~11장 콘텐츠를 추가한다.
4. 목차, 학습 내용, 진행 저장, 업데이트 내역을 연결한다.
5. CodeMirror 편집기와 출력 콘솔을 구현한다.
6. Pyodide Worker 실행, 출력 캡처, 시간/출력 제한, 중지를 구현한다.
7. 도전 과제 피드백과 흔한 오류 도움말을 연결한다.
8. 개념 일러스트를 생성하고 프로젝트 자산으로 보관한다.
9. 단위·렌더링·빌드 검증 후 브라우저 학습자 흐름을 확인한다.
10. 발견한 문제를 고친 뒤 최종 검증 결과를 보고한다.

## 9. 검증 기준

### 정적·자동 검사

- TypeScript 오류 0건
- ESLint 오류 0건
- 프로덕션 빌드 성공
- 소스 파일 500줄 미만
- 콘텐츠 ID 중복 없음
- 모든 장에 목표, 시작 코드, 도전 과제, 다음 단계가 존재

### 단위·컴포넌트 검사

- 완료 진도 저장과 복원
- 코드 초기화
- 출력/오류/실행 상태 구분
- 오류 유형별 한국어 도움말
- 도전 과제 출력 검사
- Worker 시간 초과와 출력 제한 처리
- 업데이트 내역과 출처 표시

### 실제 브라우저 학습자 흐름

1. 첫 화면에서 2장 첫 소단원의 목표와 실행 버튼을 찾는다.
2. 기본 코드를 실행해 오른쪽 아래 출력 창에서 결과를 본다.
3. 코드를 수정하고 변경된 출력을 확인한다.
4. 문법 오류를 만들고 원문 오류와 한국어 도움말을 확인한다.
5. 학습 완료 후 다음 소단원으로 이동하고 새로고침 뒤 진도를 확인한다.
6. 11장 프로젝트로 이동해 종합 예제를 실행한다.
7. 무한 반복 코드를 실행하고 중지/자동 제한 뒤 실행기가 복구되는지 확인한다.

### 반응형·접근성 검사

- 320, 375, 768, 1024, 1280px에서 가로 스크롤 없음
- 모바일 탭 전환 후 학습 내용과 코드가 모두 접근 가능
- 논리적 Tab 순서와 명확한 `:focus-visible`
- 버튼에 텍스트 이름과 상태 설명 제공
- 색상만으로 성공·오류를 전달하지 않음
- `prefers-reduced-motion`에서 `gi-pulse` 애니메이션 제거
- 라이트 모드 유지
- VoiceOver 구현 및 검증은 범위에서 제외

## 10. 수용 기준

- 2~11장이 왼쪽 목차에 순서대로 표시된다.
- 가운데에서 선택한 학습 내용을 읽고 이전/완료/다음으로 이동할 수 있다.
- 오른쪽 상단 편집기에서 코드를 수정하고 실행할 수 있다.
- 오른쪽 하단에서 출력과 오류를 실행 직후 확인할 수 있다.
- 코드 중지, 초기화, 시간 제한, 출력 제한이 작동한다.
- 진행 상태와 마지막 코드가 같은 브라우저에서 복원된다.
- 업데이트 내역과 교재 라이선스 출처가 앱 안에서 확인된다.
- 핵심 흐름 자동 검사, 빌드, 실제 브라우저 확인이 모두 완료된다.

## 11. 위험과 대응

- Pyodide 초기 다운로드가 느림: 준비 상태와 재시도 UI, 캐시 활용
- 무한 반복으로 Worker 정지: 5초 타임아웃 후 Worker 교체
- GitHub Pages에서 SharedArrayBuffer 헤더 제약: 인터럽트 버퍼 대신 Worker 종료를 기본 중지 방식으로 사용
- 모바일에서 3열이 지나치게 좁음: 목차 서랍과 학습/실습 탭 전환
- 교재 표현이 중학생에게 어려움: 원문을 짧게 재서술하고 용어 풀이 제공
- 라이선스 누락: 업데이트/출처 화면과 README에 원저자·번역자·라이선스 링크 표시
- 콘텐츠 양으로 파일 비대화: 장별 데이터 파일과 기능별 컴포넌트 분리

## 12. 롤백 방법

- 아직 Git 저장소가 아니므로 구현 전 상태는 PDF와 이 계획 문서뿐이다.
- 구현 파일은 계획 문서와 PDF를 수정하지 않고 새 파일로 추가한다.
- 외부 이미지 자산은 버전이 드러나는 새 이름으로 저장한다.
- Git 초기화, 커밋, 푸시, 배포는 별도 요청 전까지 수행하지 않는다.
